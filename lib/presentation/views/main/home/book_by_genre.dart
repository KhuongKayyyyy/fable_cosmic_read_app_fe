import 'package:flutter/material.dart';

class BookByGenre extends StatelessWidget {
  const BookByGenre({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
